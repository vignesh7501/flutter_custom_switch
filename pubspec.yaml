name: flutter_custom_switch
description: A Flutter package for both android and iOS which provides Custom switch widget.
version: 0.0.4
homepage: https://github.com/vignesh7501/flutter_custom_switch

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
